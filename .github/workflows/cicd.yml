# .github/workflows/cicd.yml

name: Build and Deploy to Minikube

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        id: build-image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/public-garden-app:${{ github.sha }}

      # Step 1: Securely write the kubeconfig to the runner's temporary directory.
      - name: Set up Kubeconfig
        run: |
          "${{ secrets.KUBE_CONFIG }}" | Set-Content -Path "$env:RUNNER_TEMP\kubeconfig"

      # Step 2: Use the temporary kubeconfig file from the runner's temp directory.
      - name: Deploy to Minikube
        run: |
          echo "Deploying to Minikube..."
          kubectl set image deployment/public-garden-deployment public-garden-container=${{ secrets.DOCKERHUB_USERNAME }}/public-garden-app:${{ github.sha }} -n public-garden-app --kubeconfig="$env:RUNNER_TEMP\kubeconfig"

